"""shipit.install — the managed-unit domain behind ``shipit install``.

``units`` is the catalog, ``splice`` the pure text splicers, ``reconcile``
the decision core and its one read boundary, ``apply`` the one effectful
path. Rendering lives at the verb.
"""
