`shipit spawn subagent` creates a Tree and launches a backend-agent Run.

Standalone issue runs are cut from `origin/main`; epic workstream runs are cut
from the epic umbrella branch and target that branch. The Run works in its Tree,
opens a draft PR, and reports linkage for the coordinator to drive.
