`shipit hook sessionstart` activates repository tooling for the session.

It writes activation into `CLAUDE_ENV_FILE`, exports log context, emits a session
event, and warns when the session appears to be running in a source clone instead
of an isolated Tree.
