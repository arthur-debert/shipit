
schema = 1
id = "singlepass"
baseline = "fanout-baseline"
axis = "pipeline shape: single-pass vs fanout"
description = "Single-pass treatment of fanout-baseline: identical invocation and sweeps, one monolithic review pass instead of the concern-scoped fan-out."

[fixture]
version = 39
prs = ["core-440", "app-391", "lex-820"]

[pipeline]
shape = "single"

[invocation]
backend = "codex"
model = "pro"
timeout = "600s"

[sweeps]
count = 2
mode = "blind"
replicates = 2
